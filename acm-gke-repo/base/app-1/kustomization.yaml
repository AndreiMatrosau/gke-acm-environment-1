---

namespace: app-1-base

resources:
- namespace.yaml
- deployment.yaml
- service.yaml

configMapGenerator:
- name: nginx-cm
  namespace: app-1-nginx
  files:
  - cm-index.html

generatorOptions:
  disableNameSuffixHash: true

