---

namespace: namespace-1

resources:
- namespace.yaml
- deployment.yaml
- service.yaml

configMapGenerator:
- name: nginx-cm
  namespace: namespace-1-base
  files:
  - cm-index.html

generatorOptions:
  disableNameSuffixHash: true

